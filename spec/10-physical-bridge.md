# 10. The Physical Bridge

Where delivery is paper, plaintext exists between decrypt and collection. The Protocol does not claim this window away; it compresses and instruments it. Requirements: render MUST occur on a certified device credentialed for the render capability, directly to the output path (no export, no file at rest); the render event MUST record page and copy counts, and collection MUST reconcile against those witnessed counts; the watermark — embedded before sealing — carries provenance across the barrier, so any photograph or copy of rendered output attributes its context; and the plaintext interval MUST be bracketed by room.sealed and distribution.completed events attested by the observer. The unlock window is per-context metadata sized to rendering logistics (duplicator-class printing for short papers; wider windows or district render hubs for booklet-scale exams; none of this changes the Protocol — only claims). Device delivery retires the bridge entirely: decrypt-in-app, render-on-screen, zeroise-on-submit.
